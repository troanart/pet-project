import { CategoryClassPipe } from './category-class.pipe';

describe('CategoryClassPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryClassPipe();
    expect(pipe).toBeTruthy();
  });
});
